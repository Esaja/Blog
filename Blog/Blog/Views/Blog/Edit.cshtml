@model Blog.Models.Blogi

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Blogi</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Otsikko)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Otsikko)
            @Html.ValidationMessageFor(model => model.Otsikko)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Teksti)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Teksti)
            @Html.ValidationMessageFor(model => model.Teksti)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
